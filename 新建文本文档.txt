if 
elif
是二者执行其一

if
if
可以一个接一个的执行下去

1.6
当return为Fall值的时候，消去

因为已经指定好要调用的定义函数了，所以函数名称有冲突也不怕

如果一个函数要有两个参数，但是你只传输了一个参数，那么第二次传输过去的参数会算是第二个参数

两个相同的定义函数重新赋值有点像这个
x=3
y=x
x=4
print(y)=3,因为赋值的是x=3


定义函数里面的定义函数，除非严格执行到了它里面的打印，不然并不会真的打印出来，而是保存在内存里面，就是如果只是按照上下顺序执行函数里面的函数，并不会执行函数里面函数的值而是会接着执行下一句，按照缩进，比如：
    def f(x):
        def g(y):
            x = y
        g(4)
        return x

    print(f(3))



lambda 函数的运用： one-argument 式函数。

    要分清返回的是函数还是值（skill：Draw frame），只有（）才是call expression
    eg： lambda【函数】 x【自变量】：x【返回值】
    当函数F的参数是函数func时，一般return lambda x来输入func 的参数x。


define


